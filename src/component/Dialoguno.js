import React from 'react';

const Dialoguno = () => {

  return (
    <Dialog open={} onClose={} aria-labelledby="">
      <DialogTitle id=""></DialogTitle>
      <DialogContent>
        <DialogContentText>
          
        </DialogContentText>
      </DialogContent>
      <DialogActions>
        <Button onClick={} color="default">
          Cancel
        </Button>
      </DialogActions>
    </Dialog>
  )
}

export default Dialoguno;
