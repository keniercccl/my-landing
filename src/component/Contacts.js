import InstagramIcon from '@material-ui/icons/Instagram';
import { IconButton } from '@material-ui/core';
import React from 'react';

const Contacts = () => {
    return (

        <div>
            <IconButton aria-label="instagrams">
                <InstagramIcon/>    
            </IconButton>
        </div>

    )
}

export default Contacts;